# music-player 项目交接文档

> 交接给 coco（coding agent）· 2026-08-12 · 转交人：Soso (lanco)

## 项目背景

大象同学（用户）想要一个**音乐播放器**，需求演进自之前的 karaoke 点唱项目（`/Users/dax/codes/karaoke/`）。

### 核心需求（用户原话归纳）

1. **连播模式 = 普通音乐播放器**：
   - 正常的播放列表
   - 播放/暂停/上一首/下一首等控制按钮
   - 中间显示专辑图片（封面）
   - 下面显示歌词（随播放滚动高亮）
   - 歌曲库可以选择本地文件夹（用户音乐在 `/Users/dax/Library/Mobile Documents/iCloud~dev~clq~Cosmos-Music-Player/Documents`，84 首歌）

2. **跟唱模式 = 逐句练习**（沿用现有 karaoke 的逻辑）：
   - 点到哪句唱哪句，点句子跳转播放
   - 每句显示 日文原文/罗马音/中文 或 原文/翻译
   - 变速（0.75/1.0/1.25）、跟唱开关

### 技术选型（已定，用户认可方向）

- **后端：Python FastAPI** + uvicorn + mutagen（读内嵌封面）
- **前端：Vue 3** + Vite（播放器 UI 组件化）
- 本地运行，浏览器访问

## 已完成（Soso 做了的）

项目目录：`/Users/dax/codes/music-player/`

- `backend.py`（228 行）：FastAPI 后端
  - `GET /api/songs` 扫描歌曲库
  - `GET /api/library` / `POST /api/library` 查看/设置歌曲库路径
  - `GET /api/cover?path=` 提取内嵌封面（mutagen APIC/MP4 covr），无内嵌时用 cover.jpg/folder.jpg
  - `GET /api/lyric?path=` 解析同名 .srt/.lrc 歌词
  - `GET /api/audio?path=` 音频流（FileResponse 原生支持 Range/206）
  - 静态托管 dist/（前端构建产物）
- `venv/`：Python 虚拟环境（fastapi/uvicorn/mutagen 已装好）
- 启动方式：`./venv/bin/python backend.py [歌曲库路径]`，默认端口 8765

## 已知问题 / 坑（重要）

1. **后端测试时有 KeyError**：`/api/songs` 返回的歌曲字段缺 `ext` 等——扫描逻辑 `rglob` + `relative_to(ROOT)` 可能有 bug，需要 coco 修复后重新测试（`path` 字段应该是绝对路径，注意跨盘 relative 计算）
2. **端口冲突**：旧 karaoke server 占用 8765，需要先 `pkill -f server.py` 或换端口
3. **Range 请求**：旧项目踩过坑——SimpleHTTPRequestHandler 不支持 Range 导致音频无法 seek；FastAPI 的 FileResponse 原生支持，但记得验证 206
4. **SRT 解析坑**（旧项目经验）：段落标题 `# 主歌1` 必须独立成块（前后空行），否则播放器会把标题+句子粘一起吞掉第一句；srt 每句可 1~3 行（原文/罗马音/中文）
5. **连播不 seek**：用户明确要求连播=整首歌自然播放，中间不能有截断/seek，时间戳只用于高亮定位

## 参考资产

- 旧 karaoke 项目：`/Users/dax/codes/karaoke/`（可参考 UI 交互、SRT 格式、Range 修复经验）
- 旧点唱页（已废弃但可参考交互）：`~/.openclaw/media/outbound/yakimochi/origin/`
- 歌曲库默认路径：`/Users/dax/Library/Mobile Documents/iCloud~dev~clq~Cosmos-Music-Player/Documents`
- 歌词缓存：`~/.cache/qqplayer/lyric/`（在线获取的歌词自动缓存）
- ⚠️ 测试数据不入仓库（歌曲文件太大，2026-08-12 已从 `tests/songs/` 删除）：`tests/test_backend.py` 用 `tmp_path` 现场生成假 mp3/srt，不依赖真实音频；有外部贡献者后再考虑样例数据

## 下一步建议

1. 修 backend.py 扫描 bug，跑通 /api/songs + /api/cover + /api/lyric + /api/audio
2. 搭 Vue 3 + Vite 前端骨架（播放列表/控制栏/封面/歌词组件）
3. 实现连播模式（音乐播放器）+ 跟唱模式（逐句练习）双模式切换
4. 测试 + 交付

有问题随时找 Soso 或直接问大象同学。
