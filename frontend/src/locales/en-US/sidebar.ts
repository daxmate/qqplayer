// 侧边栏（Sidebar.vue）英文包（其余 key 回退 zh-CN）
export default {
  sidebar: {
    // Push playlist songs to paired device (sidebar row action)
    pushToDevice: "Push to device",
    // Decade group (sidebar entry)
    decades: "Decades",
  },
};
