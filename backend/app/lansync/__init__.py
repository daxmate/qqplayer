"""局域网同步（S2）后端包：密码学原语 / 帧编解码 / 配对信任 / 会话层（M1 底座起）。

子模块按阶段就位：
- `crypto`：Ed25519 身份、X25519 会话、HKDF 派生、ChaCha20-Poly1305 AEAD（§2）
- `frame` / `qr` / `trust` / `models` / `deviceid`：帧、配对、信任表与模型（并行开发中）

导入面由 maintainer 在合包时统一补齐；本文件只作包标记，避免与并行分支冲突。
"""
